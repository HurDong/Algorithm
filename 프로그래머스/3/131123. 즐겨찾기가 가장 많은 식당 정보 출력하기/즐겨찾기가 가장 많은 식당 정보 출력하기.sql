select food_type, rest_id, rest_name, favorites from (select food_type, rest_id, rest_name, favorites, rank() over( partition by food_type order by favorites desc) as rnk from rest_info) t where t.rnk = 1 group by food_type order by food_type desc;